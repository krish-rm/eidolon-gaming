# Eidolon Predictive Intelligence Engine — Minimal Requirements
# Required for real-time WebSocket/REST API and Configuration:

fastapi>=0.100.0      # REST & WebSocket framework
uvicorn>=0.20.0       # ASGI server
pydantic>=2.0.0       # Validated configuration models
pyyaml>=6.0           # YAML file parsing
